# API Endpoints

## HTML API
### Root
+ `GET` / - loads React web app

## JSON API

### Users
+ `POST` /api/users
+ `PATCH` /api/users
+ `DELETE` /api/users/:id

### Session
+ `POST` /api/session
+ `DELETE` /api/session

### Dessert
+ `POST` /api/dessert
+ `GET` /api/dessert/:id
